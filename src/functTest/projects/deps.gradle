ext.deps = [junit: 'junit:junit:4.12',
            clover: 'org.openclover:clover:4.2.0']

// Set a specific test build directory location provided
// via property from the AbstractFunctionalTestBase
buildDir = testBuildDir
